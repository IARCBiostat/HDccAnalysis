% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_age_rate_tbl.R
\name{generate_age_rate_tbl}
\alias{generate_age_rate_tbl}
\title{Reformat age-group incidence data into an age-rate lookup table}
\usage{
generate_age_rate_tbl(
  dat,
  trage_col = "trage_invs",
  rate_col = "tx_inc_2008",
  open_hi = 120,
  interval = c("closed", "half-open")
)
}
\arguments{
\item{dat}{A data.frame with columns \code{trage_invs} and \code{tx_inc_2008}.}

\item{trage_col}{Name of the age-band column.}

\item{rate_col}{Name of the incidence-rate column.}

\item{open_hi}{The upper bound to use for open-ended bands like \code{"[85;++]"}.
Must be finite; default 120.}

\item{interval}{One of "closed" (default) or "half-open".
\itemize{
\item "closed": interprets \code{"[15;19]"} as inclusive years 15–19, so age_hi = 20
\item "half-open": interprets as \verb{[15, 19)}, so age_hi = 19
}}
}
\value{
A data.frame with columns: age_lo, age_hi, rate.
}
\description{
Reformat age-group incidence data into an age-rate lookup table
}
\examples{
ex <- data.frame(
    trage_invs = c("[15;19]", "[20;24]", "[85;++]"),
    tx_inc_2008 = c(2.5e-06, 6.0e-06, 4.4e-03)
)
make_age_rate_tbl(ex)
}
